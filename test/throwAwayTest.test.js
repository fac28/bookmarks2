const test = require("node:test");
const assert = require("node:assert");
const { compare } = require("bcryptjs");

test("throwAwayTest working", () => {
  assert.equal(1, 1, `Expected 1 to equal 1`);
});
